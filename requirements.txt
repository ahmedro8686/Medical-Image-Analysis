# مكتبات أساسية للحسابات العلمية
numpy==1.26.4      # أحدث نسخة مدعومة على Python 3.11
scipy==1.10.1      # نسخة متوافقة مع Python 3.11
pandas==2.1.1      # نسخة مدعومة على Python 3.11

# مكتبة للرسم البياني والتصوير البياني
matplotlib==3.7.2  # نسخة متوافقة مع Python 3.11

# مكتبات تعلم عميق
torch==2.8.0        # PyTorch الأساسية
torchvision==0.23.0 # دعم الصور لـ PyTorch

# مكتبة واجهة المستخدم
streamlit==1.22.0  # النسخة المستقرة الأخيرة المتوافقة مع باقي المكتبات

# مكتبات معالجة الصور
Pillow==10.4.0                 # معالجة الصور الأساسية
opencv-python-headless==4.8.1.78  # OpenCV بدون واجهة رسومية (Headless)

# ملاحظات مهمة:
# 1. تأكد من استخدام Python 3.11 لتجنب مشاكل البناء من المصدر.
# 2. يفضل تحديث pip قبل التثبيت: pip install --upgrade pip
# 3. لا تغير نسخ المكتبات إلا إذا كنت متأكد من التوافق.
